services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB:       compuroom
      POSTGRES_USER:     compuuser
      POSTGRES_PASSWORD: compupass123
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U compuuser -d compuroom"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      DB_HOST:     db
      DB_PORT:     5432
      DB_NAME:     compuroom
      DB_USER:     compuuser
      DB_PASSWORD: compupass123
      PORT:        3001
    ports:
      - "3001:3001"        # ← เพิ่มบรรทัดนี้
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  pg_data: